apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana-mcp
  namespace: mcp
spec:
  parentRefs:
    - name: traefik-gateway
      namespace: traefik
      sectionName: https
  hostnames:
    - grafana-mcp.home.zaramihnea.dev
  rules:
    - backendRefs:
        - name: grafana-mcp
          port: 3000
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: kubernetes-mcp
  namespace: mcp
spec:
  parentRefs:
    - name: traefik-gateway
      namespace: traefik
      sectionName: https
  hostnames:
    - kubernetes-mcp.home.zaramihnea.dev
  rules:
    - backendRefs:
        - name: kubernetes-mcp
          port: 8080
