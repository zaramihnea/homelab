grafana:
  url: http://kube-prometheus-stack-grafana.monitoring.svc.cluster.local
  apiKeySecret:
    name: grafana-mcp-apikey
    key: api-key

image:
  tag: "0.8.2"

service:
  port: 8000

resources:
  requests:
    cpu: 5m
    memory: 64Mi
  limits:
    cpu: 100m
    memory: 128Mi
